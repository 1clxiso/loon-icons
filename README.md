# loon-icons

Loon 插件公共图标库，共 **420** 个图标。

## 索引文件

- `metadata/icons-all.json` — 完整图标索引（JSON，含 name + url）
- `metadata/icons.list` — 文件名列表

## 使用

在 Loon 插件 `#!icon=` 行引用：

```
#!icon=https://raw.githubusercontent.com/1clxiso/loon-icons/main/图标文件名.png
```
